# 러브레이스 대시보드 디자인 분석 — 네이버 카페 "러브레이스 업데이트 with CODEX"

> 출처: m.cafe.naver.com/koreassistant/23929 (작성자: 프레이, 2026-08-10)
> 참조 이미지: `reference-images/img_00.jpg` ~ `img_14.jpg` (15장 전량, Galaxy Fold 7 펼친 화면 기준)
> 목적: 순정 Home Assistant에서 이 디자인을 재현하고, 추후 오픈소스로 공유하기 위한 레퍼런스 분석

---

## 1. 전체 디자인 시스템

### 1.1 컬러 팔레트
| 역할 | 값(추정) | 비고 |
|---|---|---|
| 페이지 배경 | `#F0EDE7` ~ `#F4F1EA` | 따뜻한 크림/베이지 톤. HA 기본 다크/라이트와 완전히 다른 "에디토리얼" 무드의 핵심 |
| 카드 본문 배경 | `#FFFFFF` | 크림 배경 위에 떠 있는 흰 카드 |
| 본문 텍스트 | `#1A1A18` (거의 검정) | 약간 따뜻한 블랙 |
| 보조 텍스트 | `#8A8578` | 회갈색 뮤트 톤 (단위, 캡션, 서브텍스트) |
| 강조-골드/앰버 | `#E8A33D` 계열 | 에너지 차트 바, 조명 ON 아이콘, A/C 아이콘 |
| 강조-블루 | `#3D6FE0` 계열 | 냉방/온도 칩, 오늘 데이터(차트), 물 관련 |
| 강조-틸/시안 | `#0E9AA7` 계열 | HA 토글 스위치 ON 상태 |
| 강조-그린 | `#2FA36B` 계열 | 잠김(도어락), 배터리 정상, 온라인 상태 점 |
| 강조-퍼플 | `#8B5CF6` 계열 | 커튼 카드 아이콘 |
| 슬라이더 핸들 | 로즈/레드 | 볼륨·물결 속도 슬라이더 |

핵심: **도메인별 액센트 컬러**가 카드마다 일관되게 적용됨 (조명=골드, 냉방=블루, 도어락/배터리=그린 등).

### 1.2 타이포그래피
- 한글 산세리프 — Pretendard 또는 Noto Sans KR 추정. 폰트 굵기 활용이 극단적.
- **아이브레이(eyebrow) 라벨**: 영어 대문자 + letter-spacing 넓게 (`SAMSUNG · WIND-FREE AI · 2026`, `LIGHTING`, `ENERGY CONTROL`, `BEACON NETWORK`) + 좌측 미니 아이콘
- **헤드라인**: 한글 ExtraBold ~20-26px, 문장형 ("안방 조명이 켜져 있어요")
- **상태 행**: 라벨 regular / 값 semibold 우측 정렬
- **히어로 내 텍스트**: 흰색, 사진 위 하단 그라데이션(`rgba(0,0,0,.55) → transparent`)으로 가독성 확보

### 1.3 레이아웃
- 모바일(Fold 7 펼침) 기준 **2단 그리드**, 열 간격 ~16px, 페이지 좌우 패딩 ~16px
- 카드 border-radius ~24px (큼직하고 부드럽게), shadow는 매우 연하게 (y-offset 큰 soft shadow)
- 카드 상단에 HA 기본 헤더(탭 바): 홈/이력/에너지/카메라/미디어/선물(?)/침실/식기/차량/XGIMI/스피커 아이콘 탭 — **HA 기본 view 탭을 그대로 활용** (아이콘만)

### 1.4 카드 해부학 (모든 카드 공통 문법)
```
┌─────────────────────────────┐
│  [히어로: AI 생성 디바이스/공간 사진]  │  ← 상태를 반영한 사진 (조명 ON=밝은 방, OFF=어두운 방)
│  ● 상태배지pill (우상단)      │
│  EYEBROW 라벨                │
│  문장형 한글 헤드라인          │
│  [칩][칩][칩] ← 상태 요약 pill │
├─────────────────────────────┤
│ (icon) 라벨 ·········· 값     │  ← 아이콘: 도메인 색 소프트 틴트 원형/라운드 백그라운드
│ (icon) 라벨 ·········· [토글] │  ← 값 or HA 토글 or pill 버튼 or 슬라이더
│ ...                          │
└─────────────────────────────┘
```

**핵심 디자인 아이디어 3가지**
1. **상태=문장**: "무엇이 켜져 있어요"처럼 상태를 대화체 헤드라인으로 요약 (숫자 나열이 아니라 사람에게 말하듯)
2. **상태 반영 이미지**: 히어로 사진이 실제 기기 상태를 반영 (조명 꺼짐=어두운 방 사진, 커튼 닫힘=닫힌 창 사진) — AI 생성 이미지를 상태별로 여러 장 만들어 조건부 표시
3. **행(row) 그래머 통일**: 모든 카드 본문이 "티ント 아이콘 + 라벨 + 우측 값/토글" 동일 구조 → 카드가 달라도 읽는 법이 같음

---

## 2. 카드 유형별 상세 분석 (15장 전량)

| # | 파일 | 카드 | 특징 |
|---|---|---|---|
| 0 | img_00 | **날씨 (서울 송파구)** | 야경 사진 히어로, 초대형 온도 "28°", 날씨 아이콘+구름 문장 헤드라인, 칩(습도 65%/이슬점 21°C/바람), 하단 5일 예보 미니 타일(오늘~목, 고저온도+아이콘). 우상단 "맑음·실시간" 배지 |
| 1 | img_01 | **날씨 (호텔)** + 가족 재실 | 두 번째 지역 날씨 카드(27°, 흐림). "FAMILY PRESENCE · LIVE": 일러스트 히어로 + "오늘도 우리 가족, 함께예" 헤드라인, 가족 구성원 리스트(아바타+재실/자리—상태), 초록 재실 점 |
| 2 | img_02 | **차량 — Mercedes GLB 200** | 다크 브라운 그라데이션 히어로 + 차량 컷아웃 렌더, "비론 미설치 구역"(주차 위치) 헤드라인, 행: 현재 주차 위치/차량 상태(주차됨)/마지막 운전자(엄마)/주차 시간 |
| 3 | img_03 | **에어컨 — Samsung WindFree AI** (안방) | 밝은 실내 사진 히어로(천장 AC), "1개 냉을 쾌적하게", 칩(AI 자동/28.3°C), 행: 운전 조절(목표 26°C)/각 방 온도조절 목록(거실·안방·등 현재/목표 온도) |
| 4 | img_04 | **에너지 컨트롤** | 다크 히어로(집 단면 렌더), "ENERGY CONTROL", 실시간 전력 **1.15kW** 대형 숫자, "실시간 연동" 배지, 게이지 링(누적 전기요금 226,140원, 목표 100,000원 대비 226%, 로즈레드 링), 3개 스탯 타일(이번달 사용량 263kWh/요금 40,730원/예상 821kWh) |
| 5 | img_05 | **조명 — 안방(ON)** | 어두운→밝은 방 사진, "LIGHTING", "3개 켜짐" 배지, 행: 천장/리모델링?/입구/드레스룸/드레스룸 옷 각각 토글 |
| 6 | img_06 | **에어컨 — 안방 상세** | WindFree 제품 사진 히어로, 칩(켜짐/29°C/47%/51W), 행: 자동 운전(목표 27.5°C)/현재 온도/습도 47%/에어컨 전력 51W |
| 7 | img_07 | **커튼 — 안방(열림 100%)** | 커튼 사진 히어로, "안방에 빛이 가득 들어와요", 칩(열림/열림 100%), 본문: 개폐 버튼(열기↑/닫기↓) + 위치 버튼(위로/아래로), 퍼플 커튼 아이콘 |
| 8 | img_08 | **조명 — OFF 상태 + 커튼(닫힘)** | 조명 카드가 어두운 방 사진(꺼짐 상태 반영!), "전체 켜짐" 배지, 토글 OFF. 우측 욕실 조명 카드(욕실 조명/욕실등/환풍기 토글) |
| 9 | img_09 | **도어락 — Samsung SHP-DP960** | 다크 제품 사진 히어로, "한란이 안전하게 잠겨요", 그린 "잠김" 칩 + 배터리 100% + 이상 없음 칩, 행: 잠금 해제(파란 액션 링크!)/도어락 배터리 100%/이상 감지 해제 |
| 10 | img_10 | **에너지 분석 페이지** | "우리집 에너지 분석", 기간 세그먼트 탭(일간/주간/월간/분기 — 주간 선택), 주간 막대차트(월~일, 골드 바 + 오늘만 블루 + 평균 점선), KPI 4타일(기간 총사용량 191kWh/일평균 27.2kWh/최고 사용일/월별 예상요금 226,140원), **가전별 사용량 리스트**: 2단 그리드, 틴트 아이콘 + 기기명 + 현재 W + 누적 kWh + 프로그레스 바 (키스커 에어컨 100kWh 최상위…) |
| 11 | img_11 | **로봇청소기 — Dreame X60 (상태)** | 제품 사진 히어로, "CLEANING PROFILE · 스테틴 청소 설정", 셀렉트 드롭다운 필드들(청소 방식: 정지 및 몸굴리기 청소 / 흡입 세기: 표준 / 청소 정도: 표준 청소 / CleanGenius: 개집), 물결의 속도 슬라이더(15) |
| 12 | img_12 | **로봇청소기 — BASE & CARE + 지도** | 좌: 라이브 맵 카드(방별 색상 세그먼트 + 로봇 위치 마커). 우: "충전·소모품 관리" — 충전 상태(충전 완료)/스테이션 상태(대기 중)/깨끗한 물통(장착됨)/오수통(장착됨)/먼지봉투(장착됨)/세제(장착됨) 유지보수 행 리스트, "브러시 96%·필터 72%" 칩 |
| 13 | img_13 | **식세기 — Samsung Chef Collection** | 식기세척기+오븐 2카드: 라이트 그레이 제품 렌더 히어로("식기세척 준비 완료"/"요리를 기다리는 중"), 중앙 인셋 파워 타일(현재 상태: 꺼짐 + 파워버튼), 행: 식기세척기 상태(껴짐)/잔여건·작동·작동 시간(준비)/아동·잠금(껴짐) |
| 14 | img_14 | **미디어 — Google Home Mini + XGIMI 프로젝터** | 좌: XGIMI 리모컨 카드(렌더된 리모컨 이미지 + 되감기/정지/재생/빨리 pill 버튼, "연결됨" 배지). 우: Plex 재생중 카드(XGIMI Horizon S Max, 포스터 썸네일 + 진행률, 1개 실행 배지). Google Home 카드: 다크 스피커 사진 히어로에 **트랜스포트 버튼이 히어로 하단에 임베드**(이전/-10/재생/+10/다음/정지), 본문: 자동 연속재생 토글/볼륨 슬라이더(20%)/금지 시간(hh:mm AM/PM 셀렉트)/음악 보관함(37곡) |

**특이 구조 포인트**
- **미디어 카드의 히어로 임베드 컨트롤**: 재생 버튼을 흰 본문이 아니라 어두운 사진 히어로 안에 넣어 "미디어 기기"임을 즉시 전달
- **상태 대비 사진 교체**: 같은 조명 카드가 켜짐(img_05 밝은 방)과 꺼짐(img_08 어두운 방)에서 히어로 사진이 다름 → 상태 기반 이미지 분기
- **차량의 생태계**: 차량 카드가 위치/주차/운전자/Android Auto/주차 지도/비콘 네트워크(3/3 정상)/B1·B2 비콘 헬스까지 하나의 "차량" 스토리로 확장
- **액션의 계층**: 정보성 행(값 표시) vs 액션성 행(토글/버튼/슬라이더/셀렉트/액션 링크)이 같은 행 그래머 안에서 섞임

---

## 3. 순정 Home Assistant 재현 경로 (무튜닝 사용자용)

이 디자인은 순정 HA로는 불가능하고 **커스텀 카드 조합**으로 재현합니다. 최소 조합:

### 3.1 필수 HACS 컴포넌트
| 컴포넌트 | 역할 |
|---|---|
| `custom:button-card` | 모든 카드의 기반. 템플릿+JS Jinja로 히어로/칩/행 그래머 구현. extra_styles로 radius/shadow/폰트 |
| `custom:card-mod` | HA 기본 카드·헤더·탭 바 CSS 주입(크림 배경, 탭 아이콘 스타일, 폰트) |
| `custom:layout-card` 또는 **새 Sections 대시보드** | 2단 그리드. Fold 펼침 등 넓은 화면에서 2단, 좁으면 1단 반응형 |
| `custom:mini-graph-card` 또는 apexcharts-card | 에너지 주간 막대차트(오늘 다른 색, 평균선) |
| `custom:valetudo-map-card`(또는 vacuum용 맵카드) | 로봇청소기 방별 세그먼트 맵 |
| `custom:stack-in-card` | 히어로+본문을 한 카드로 묶기 |
| 테마 | `frontend:` 에서 Pretendard 폰트 + 크림 팔레트 primary/背景 색 지정 |

### 3.2 구현 순서 (순정에서 시작하는 로드맵)
1. **테마 먼저**: `/config/themes/warm-editorial.yaml` — 배경 #F0EDE7, 카드 흰색, 폰트 Pretendard, text/muted 색. `frontend: themes: !include_dirmerge_named themes`
2. **button-card 템플릿 3종**: `(a) hero-header`(사진+그라데이션+eyebrow+문장헤드라인+배지), `(b) chip-row`(아이콘 칩), `(c) status-row`(아이콘+라벨+값/토글) → JS 템플릿으로 만들고 모든 카드가 재사용
3. **상태 반영 이미지**: 기기별 ON/OFF 사진 세트를 `/config/www/devices/<device>/on.jpg|off.jpg`로 넣고 `[[entity]]` state로 이미지 분기 (`picture` 템플릿)
4. **카드 조립**: 날씨(기본 weather+커스텀 히어로) → 조명/커튼(토글+버튼) → 에너지(mini-graph+게이지: custom:ring-chart 또는 button-card 원형 SVG) → 로봇청소기(맵카드+셀렉트) → 차량(device_tracker+sensor 행) → 미디어(media_player 카드, 트랜스포트 = button-card)
5. **탭 바**: HA 기본 탭에 아이콘 view들(도메인별 대시보드 view 분리) — 이미 원본도 HA 탭 구조 활용

### 3.3 난이도 참고
- 원본 제작자도 "HA + Codex"로 만들었고, 핵심은 HA 기본 구조(탭 바, 엔티티) 위에 **button-card 템플릿 레이어**를 얹은 것으로 보임
- AI 이미지 생성(Midjourney/DALL·E 등)으로 기기별·상태별 사진을 확보하는 게 이 디자인의 절반

---

## 4. 오픈소스 공유 패키징 제안

```
ha-design/
├── README.md                 ← 스크린샷 + 요구 컴포넌트 표
├── themes/warm-editorial.yaml
├── lovelace/templates/       ← button-card 템플릿 (hero/chip/row)
├── dashboards/lovelace.yaml  ← 예시 대시보드 전체
├── www/images/               ← AI 생성 이미지 세트 (직접 배포 or 생성 프롬프트 공개)
├── docs/PROMPTS.md           ← 이미지 생성에 쓴 프롬프트 (재현성 핵심!)
└── hacs.json
```

- **이미지 라이선스/생성 프롬프트 공개**가 공유 프로젝트의 차별점 (남들이 안 주는 것)
- 템플릿은 entity/state만 바꿔 끼울 수 있게 파라미터화 → 다른 집/기기에도 적용 가능
- 라이선스: CC BY-NC 또는 MIT + 이미지 별도 표기

---

## 5. 검증 (QA-by-read)
- 15장 이미지(img_00~14) ↔ §2 표 15행 1:1 대조 완료 — 날씨×2, 가족재실, 차량(GLB/Android Auto/주차지도/비콘×3), 에너지×2, 에어컨×3, 조명×3, 커튼×2, 도어락, 로봇청소기×3(상태/지도+케어/프로필), 식기×2, 미디어(GoogleHome/XGIMI 리모컨/Plex) 전량 커버 확인
- PROSE 산출물이므로 테스트 코드 없음 — 리뷰+독해 QA로 대체 (근거: 기계 소비자가 없는 순수 문서)
